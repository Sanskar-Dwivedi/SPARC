{
  "name": "sparc-vercel-deployment",
  "private": false,
  "scripts": {
    "vercel-build": "npm --prefix apps/web ci --ignore-scripts && npm --prefix apps/web run build && npm --prefix orbital-website ci --ignore-scripts && node scripts/vercel/assemble.mjs"
  }
}
